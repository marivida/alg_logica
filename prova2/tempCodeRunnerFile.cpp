for (int i = 0; i < 10; i++)
      {
        printf("\nDigite o numero da posicao %d do vetor 1: ", i);
        scanf("%d%*c", &vetor1[i]);
      }

      printf("\nVetor 1: ");
      for (int i = 0; i < 10; i++)
      {
        printf("%d -- ", vetor1[i]);
      }

      for (int i = 0; i < 10; i++)
      {
        printf("\n\nDigite o numero da posicao %d do vetor 2: ", i);
        scanf("%d%*c", &vetor2[i]);
      }

      printf("\nVetor 2: ");
      for (int i = 0; i < 10; i++)
      {
        printf("%d -- ", vetor2[i]);
      }